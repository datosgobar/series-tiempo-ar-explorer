import * as React from 'react';
import Serie from '../../common/serie/Serie'
// TODO: reemplazar default por lista vacia, esto es solo una prueba
export default function FeaturedReducer(state: any = [<Serie key={1}/>], action: any) {
    return state;
}